# Runtime Files

This directory is used by the meter recognition pipeline for generated files.

- `meter_samples/`: five-frame image captures for Docker inference.
- `meter_images/`: one-shot images used by the legacy snapshot pipeline.

These files are runtime artifacts and can be deleted between tests.
