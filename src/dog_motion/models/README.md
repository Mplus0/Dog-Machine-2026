# Meter YOLO Models

Place the meter recognition TensorRT engine here.

Default path used by launch files:

```text
comp2026_ws/src/dog_motion/models/yuyin.engine
```

Inside the Docker container this package is mounted as `/workspace`, so the
same model is referenced as:

```text
/workspace/models/yuyin.engine
```
