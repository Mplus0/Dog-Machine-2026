NavfnROS:
  # General Parameters
  allow_unknown: true
  default_tolerance: 0.5

  # Planning Window
  planner_window_x: 0.0
  planner_window_y: 0.0

  # Optimization
  visualize_potential: false
  # planner_costmap_publish_frequency: 1.0 # 默认参数，通常不需要修改
  # publish_potential: false # 默认参数，通常不需要修改