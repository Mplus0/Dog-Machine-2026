# 重力对齐地面滤波：默认启用前分阶段验证

## 1. 目的与最终决策

本文用于判断是否可以把以下实验参数改为导航任务的默认值：

```text
start_ground_filter_experimental:=true
navigation_scan_topic:=/scan_ground_filtered
```

本文只用于验收，不要求在验收期间修改滤波、AMCL、Costmap、TF 或 launch 参数。任一硬性项目失败时应停止后续阶段，保存结果，再根据证据修改代码或配置。

最终有三种结论：

- `GO-OBSTACLE`：允许先在 `task_2026_obstacle_test.launch` 中默认启用。
- `GO-ALL`：允许在 navigation、pick-place、full task 中逐步默认启用。
- `NO-GO`：保持当前显式参数启用方式，不修改默认值。

不要一步将 `stack_nav_base.launch` 改为全局默认。应先通过 `GO-OBSTACLE`，再完成全任务回归，最后考虑 `GO-ALL`。

## 2. 当前已有证据

以下项目在 2026-08-15 已有实机证据，后续可复核但不需要重新调参：

| 项目 | 已有结果 | 当前判定 |
|---|---|---|
| 编译和运行 | 新节点可在 aarch64 ROS Noetic 主机运行 | 通过 |
| 发布频率 | `/scan_ground_filtered` 约 15 Hz | 通过 |
| IMU 同步 | 常见时间差小于 10 ms | 通过 |
| 正常站立 | 地面高度约 0.412 m，诊断 `OK` | 通过 |
| 低头过滤 | 原始 574/640 束地面点，滤波后仅 7/640 束远处点 | 通过 |
| 左右倾斜 | 约 ±11.6° 时未出现地面扫描 | 通过 |
| 真实障碍保留 | 相机前约 0.54 m 障碍物可检测，移除后扫描立即恢复 | 通过 |
| 地面写入 Costmap | 低头前后致命占用栅格均为 65 | 通过 |
| 动态障碍自动清除 | 专用有限清除扫描下 local/global 动态层均快速清除 | 通过 |
| 非地图现场 AMCL | 约 3.16 cm、0.23°，恢复后不继续累积 | 暂定通过 |

尚未完成、会阻塞默认启用的项目：

- 建图现场连续三轮低头静止更新的 AMCL 验收。
- 实际步态、转向和完整障碍任务运行时的稳定性。
- 连续启动、退出、重新启动的可靠性。

### 2026-08-15 Gate 4 修复与复测记录

- 第一版使用原始 `/scan` 作为 clearing-only source。箱子移除后实测原始扫描与
  过滤扫描均为 `finite=0, nan=640`，local/global 动态障碍无法可靠清除，因此
  该方案判定失败并已替换。
- 当前版本新增 `/scan_ground_clearing`：障碍存在时有限射线截止到最近障碍；
  无障碍时截止到完整深度图中最远的已观测地面；无有效深度仍输出 `NaN`。
- 正常站立时 `/scan_ground_clearing` 约 `14.97 Hz`，有效束约
  `631–632/640`，中位距离约 `2.85–2.86 m`。
- 隔离关闭 global `static_layer` 后，0.7–1.0 m 箱子能稳定进入 filtered scan、
  local Costmap 和 global Costmap；移除后 local/global 障碍簇均快速自动清除，
  未调用 `/move_base/clear_costmaps`。
- 开启静态层时仍存在、关闭 global obstacle layer 后仍不消失的障碍簇已确认来自
  静态地图，不属于动态 clearing 失败。

结论：Gate 4 动态障碍标记和有限距离自动清除通过。重新启用静态层后再进入
Gate 5；整个 global Costmap 的静态占用数量不作为动态清除判据。

## 3. 安全要求

开始任何阶段前必须满足：

- 测试区域平整，机器人周围至少留出 1 m 空间。
- 手柄放在不易误触的位置，急停或人工接管可立即使用。
- 除明确的行走阶段外，不发送导航目标和 `/cmd_vel`。
- 姿态测试只包含正常站立、低头、恢复、左倾和右倾，不测试趴下。
- 每次只改变一个条件，动作完成后至少保持 5 秒。

出现以下任一情况立即停止当前 launch：

- 非预期趴下、行走、转向或持续非零 `/cmd_vel`。
- `/ground_filter/diagnostics` 持续为 `ERROR`。
- `/scan_ground_filtered` 长期低于 10 Hz。
- RViz 中出现整片地面障碍、坐标突然跳变或 TF 大面积丢失。
- 真实障碍物没有被扫描或 Costmap 标记。

停止命令为启动终端中的 `Ctrl+C`。不要使用 `kill -9`；只有记录失败证据后，才可按需调用：

```bash
rosservice call /move_base/clear_costmaps
```

## 4. 通用准备

以下命令中的 IP 必须替换为机器狗感知主机当前地址：

```bash
cd ~/comp2026_ws
git pull
catkin_make --pkg depthimage_to_laserscan allmovebase
source devel/setup.bash

unset ROS_HOSTNAME
export ROBOT_TEST_IP=192.168.31.83
export ROS_MASTER_URI=http://${ROBOT_TEST_IP}:11311
export ROS_IP=${ROBOT_TEST_IP}
```

建议准备三个终端和 RViz：

- 终端 A：导航 launch。
- 终端 B：扫描 A/B 监视器。
- 终端 C：检查命令和数据记录。
- RViz：同时显示原始扫描、滤波扫描、local/global Costmap、机器人模型和粒子云。

终端 A：

```bash
roslaunch allmovebase task_2026_obstacle_test.launch \
  run_obstacle_task:=false \
  start_ground_filter_experimental:=true \
  navigation_scan_topic:=/scan_ground_filtered
```

终端 B：

```bash
source ~/comp2026_ws/devel/setup.bash
rosrun allmovebase scan_ab_monitor.py
```

RViz 建议使用不同颜色：

- `/scan`：白色。
- `/scan_ground_filtered`：绿色。
- `/scan_ground_clearing`：橙色（仅用于确认有限清除距离，不作为障碍显示）。

## 5. Gate 0：构建和单元测试

机器狗和导航可以关闭。

```bash
cd ~/comp2026_ws
source devel/setup.bash
catkin_make gravity_aligned_filter_test
./devel/lib/depthimage_to_laserscan/gravity_aligned_filter_test
```

通过标准：

- 构建命令退出码为 0。
- 三个 gtest 均显示 `PASSED`。

记录：

```text
[√] PASS  [ ] FAIL
commit/HEAD:
build output summary:
```

## 6. Gate 1：启动完整性和真实订阅链

启动终端 A 后，不要移动机器人。执行：

```bash
rosnode list | grep -E 'gravity|depthimage|amcl$|move_base$|imu_throttler'
rostopic info /scan_ground_filtered
rostopic info /scan_ground_clearing
rostopic info /scan
rostopic info /imu/data_throttled
rostopic hz /scan_ground_filtered
rostopic hz /scan_ground_clearing
rostopic echo -n 1 /ground_filter/diagnostics
rosparam get /gravity_aligned_depth_to_laserscan/range_min
rosparam get /gravity_aligned_depth_to_laserscan/fallback_to_nominal_ground
```

远程 RViz 不显示滤波扫描时执行：

```bash
rosnode info /gravity_aligned_depth_to_laserscan
```

节点地址必须是当前主机 IP，不能是远程电脑无法解析的 `http://lite:端口/`。

通过标准：

- `/scan_ground_filtered` 的订阅者包含 `/amcl` 和 `/move_base`。
- `/amcl` 只订阅 `/scan_ground_filtered`，不订阅原始 `/scan`。
- `/scan_ground_clearing` 的订阅者包含 `/move_base`，不包含 `/amcl`。
- `/move_base` 不订阅原始 `/scan`；原始话题只保留用于 A/B 诊断。
- `/imu/data_throttled` 的订阅者包含滤波节点。
- 输出约 15 Hz，长期不低于 10 Hz。
- 正常可见地面时诊断中的 `clearing_scan_bins` 大于 0；无有效深度时允许为 0。
- 正常站立时诊断为 `OK`，没有持续 IMU 超时。
- 运行时 `range_min=0.1`。

记录：

```text
[ ] PASS  [ ] FAIL
publisher URI:
filtered subscribers:
raw subscribers:
frequency:
diagnostic level/message:
imu_age_sec:
```

## 7. Gate 2：静态姿态三轮重复性

每轮顺序：

```text
正常站立 5 s
低头到 pitch 12–15°并保持 5 s
恢复正常 5 s
左倾到 roll -10～-12°并保持 5 s
恢复正常 5 s
右倾到 roll +10～+12°并保持 5 s
恢复正常 5 s
```

连续执行三轮。监视终端记录每个姿态的 roll、pitch、原始扫描和滤波扫描。

通过标准：

- 三轮低头时，原始扫描可出现约 1.0–1.3 m 地面点，但滤波扫描不得出现对应的连续近距离地面弧线。
- 正常、左右倾状态下真实墙面方向正确，不出现左右镜像错误。
- 恢复后扫描回到基线，不需要清 Costmap。
- 地面高度始终位于 0.20–0.80 m，诊断通常为 `OK`。
- 任一轮不能出现持续 `WARN` 或 `ERROR`。

记录表：

| 轮次/姿态 | roll | pitch | raw 有效束/最近距离 | filtered 有效束/最近距离 | ground height | diagnostic | 结果 |
|---|---:|---:|---|---|---:|---|---|
| 1 正常 | | | | | | | |
| 1 低头 | | | | | | | |
| 1 左倾 | | | | | | | |
| 1 右倾 | | | | | | | |
| 2 | | | | | | | |
| 3 | | | | | | | |

## 8. Gate 3：近距离真实障碍扫描保留

此阶段机器人保持静止，不发送导航目标。使用宽度至少 10 cm、高度至少 10 cm、深度相机可稳定识别的障碍物。

依次把障碍物放在相机前约：

```text
0.15 m
0.25 m
0.50 m
1.00 m
2.00 m
```

0.15 m 和 0.25 m 只检查 `/scan_ground_filtered`，因为它们可能位于机器人 footprint 或 padding 内，不要求进入 Costmap。不要把手长时间留在画面内，放好后离开视野再读数。

每个距离记录：

```bash
rostopic echo -n 1 /ground_filter/diagnostics
```

同时观察绿色扫描。若需精确统计，使用终端 B 的监视输出。

通过标准：

- 五个距离均能在滤波扫描中看到障碍。
- 测得最近距离与实际距离差不超过 0.08 m。
- 放置障碍不导致地面平面持续失败。
- 障碍移除后，`/scan_ground_filtered` 中对应点在 1 秒内消失。

记录表：

| 实际距离 | 扫描距离 | 是否保留 | 移除后扫描是否消失 | 结果 |
|---:|---:|---|---|---|
| 0.15 m | | | | |
| 0.25 m | | | | |
| 0.50 m | | | | |
| 1.00 m | | | | |
| 2.00 m | | | | |

## 9. Gate 4：Costmap 动态障碍标记与自动清除（默认启用阻塞项）

这是当前最重要的未验证项目。实验闭环采用两个独立 observation source：

```yaml
laser_scan_sensor:             # /scan_ground_filtered
  marking: true
  clearing: false
ground_clearing_sensor:        # /scan_ground_clearing
  marking: false
  clearing: true
  inf_is_valid: false
```

过滤结果中的障碍端点只负责写入；同一滤波器根据完整深度图生成专用有限清除
射线：有障碍时只清到最近障碍，无障碍时清到该方位最远的已观测地面。无深度的
方位保持 `NaN`，不会被当成无限远自由空间。本 Gate 未通过时禁止修改默认参数。

### 9.1 记录空场基线

确保相机前 0.5–1.0 m 没有临时障碍，等待 10 秒。在终端 C 执行：

```bash
python3 -c "import rospy; from nav_msgs.msg import OccupancyGrid; rospy.init_node('costmap_baseline',anonymous=True,disable_signals=True); m=rospy.wait_for_message('/move_base/local_costmap/costmap',OccupancyGrid,5); print('local fatal=',sum(1 for x in m.data if x>=100),'known=',sum(1 for x in m.data if x>=0),'cells=',len(m.data))"
```

记录 RViz 局部和全局代价地图截图。

### 9.2 放置障碍

在相机前 0.5–1.0 m 放置高度至少 15 cm、宽度至少 15 cm 的障碍物，等待 5 秒。

通过标准：

- 绿色扫描出现障碍。
- `/scan_ground_clearing` 在箱子方位的距离不得穿过箱子，应与过滤障碍距离近似一致。
- local Costmap 在相同方向和距离出现障碍簇。
- global Costmap 应出现一致障碍。local/global Costmap 都应加载过滤标记源和
  专用地面清除源。
- 不能把机器人周围整片区域标成障碍。

可用以下命令核对配置和实际连接：

```bash
rosparam get /move_base/global_costmap/plugins
rosparam get /move_base/global_costmap/obstacle_layer/observation_sources
rosparam get /move_base/global_costmap/obstacle_layer/laser_scan_sensor/topic
rosparam get /move_base/global_costmap/obstacle_layer/laser_scan_sensor/marking
rosparam get /move_base/global_costmap/obstacle_layer/laser_scan_sensor/clearing
rosparam get /move_base/global_costmap/obstacle_layer/ground_clearing_sensor/topic
rosparam get /move_base/global_costmap/obstacle_layer/ground_clearing_sensor/marking
rosparam get /move_base/global_costmap/obstacle_layer/ground_clearing_sensor/clearing
rostopic info /scan_ground_filtered
rostopic info /scan_ground_clearing
rostopic info /scan
rosnode info /move_base
```

预期参数为：

```text
observation_sources: laser_scan_sensor ground_clearing_sensor
laser_scan_sensor/topic: /scan_ground_filtered
laser_scan_sensor: marking=true, clearing=false
ground_clearing_sensor/topic: /scan_ground_clearing
ground_clearing_sensor: marking=false, clearing=true
```

`/move_base` 应同时出现在两个 ground scan 话题的订阅者中，`/amcl` 只能出现在
`/scan_ground_filtered` 的订阅者中；两者均不应订阅原始 `/scan`。

再次执行上面的占用栅格统计命令，记录“有障碍”数值和截图。

### 9.3 移除障碍

移除障碍并离开相机视野，机器人保持不动，分别在 1、5、10、15 秒观察。不要调用 `/move_base/clear_costmaps`。

通过标准：

- `/scan_ground_filtered` 对应障碍点在 1 秒内消失。
- `/scan_ground_clearing` 在原箱子方位保持有限值，且距离应越过原箱子位置；
  若完整深度图在该方位确实没有有效地面观测，则允许保持 `NaN`，但该方位不能
  作为自动清除通过证据。
- local Costmap 对应障碍簇在 10 秒内自动消失。
- 当前相机视野内、障碍物原位置 ROI 的致命栅格回到放置前基线 ±5 格。
- global Costmap 中本次在同一 ROI 产生的临时障碍在 15 秒内自动消失。
- 不要求整个 global Costmap 的致命栅格总数回到基线；前向视觉无法立即更新
  当前视野外的历史区域。

记录表：

| 时刻 | filtered 障碍 | local fatal | local 障碍簇 | global 障碍簇 |
|---|---|---:|---|---|
| 空场基线 | | | | |
| 放置后 5 s | | | | |
| 移除后 1 s | | | | |
| 移除后 5 s | | | | |
| 移除后 10 s | | | | |
| 移除后 15 s | | | | |

失败处理：

- 如果扫描点消失但 Costmap 障碍不消失，记录为 `FAIL-CLEARING`。
- 不要修改 `raytrace_range`、`obstacle_range` 或 inflation 参数来掩盖问题。
- 保存 `/scan_ground_filtered`、local/global Costmap 截图和时间数据后停止测试。
- 将结果交给 Codex，优先设计“可靠清除射线/独立 clearing source”，不直接把所有空束改成无限远清除。

## 10. Gate 5：建图现场 AMCL 三轮压力测试

必须在实际建图现场执行。非地图现场的数据只能作为相对回归，不能决定默认启用。

### 10.1 基线

设置初始位姿，等待定位稳定至少 30 秒。执行：

```bash
rosrun tf tf_echo map odom
rostopic echo -n 1 /amcl_pose
```

记录 `map -> odom` 的 x、y、yaw，以及 AMCL covariance `[0]`、`[7]`、`[35]`。

### 10.2 每轮动作

机器人位置保持不动：

1. 正常站立记录基线。
2. 低头到 pitch 12–15°并保持。
3. 每隔约 1 秒调用一次，共 4 次：

```bash
rosservice call /request_nomotion_update
```

4. 再次记录 `map -> odom` 和 `/amcl_pose`。
5. 恢复正常站立，等待 10 秒，再记录一次。
6. 重复三轮。

通过标准：

- 每轮 `map -> odom` 平移变化不超过 0.03 m。
- 每轮航向变化不超过 1°。
- covariance 不持续翻倍，恢复后不继续累积。
- 三轮结束后的总偏移仍满足相同阈值。
- local/global Costmap 不产生地面障碍。

记录表：

| 轮次/时刻 | map→odom x | y | yaw | AMCL cov x | cov y | cov yaw | local fatal | 结果 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 1 基线 | | | | | | | | |
| 1 低头更新后 | | | | | | | | |
| 1 恢复后 | | | | | | | | |
| 2 | | | | | | | | |
| 3 | | | | | | | | |

## 11. Gate 6：实际步态和转向

只有 Gate 0–5 全部通过后才能执行。先使用空旷、低速、短距离测试，不直接运行完整比赛流程。

建议录包：

```bash
rosbag record -O ground_filter_walk_test.bag \
  /scan /scan_ground_filtered /scan_ground_clearing /imu/data_throttled \
  /ground_filter/diagnostics /amcl_pose /particlecloud \
  /odometry/filtered /move_base/local_costmap/costmap \
  /move_base/global_costmap/costmap /tf /tf_static
```

动作顺序：

```text
静止 10 s
直行 2–3 m
停止 10 s
左转约 90°
停止 10 s
右转约 90°
停止 10 s
返回起点附近
```

通过标准：

- 滤波输出持续不低于 10 Hz。
- 步态 pitch/roll 周期内不出现地面扇形扫描。
- 真实墙面和障碍物连续，不发生左右翻转或整体跳动。
- local/global Costmap 不累积地面障碍。
- AMCL 没有突然跳变；返回后位置误差满足任务要求。
- 全程没有持续 `WARN`/`ERROR`。

重复三次。任一次失败均不进入完整任务测试。

## 12. Gate 7：连续启动和完整障碍任务

### 12.1 连续启动

执行五轮：

1. 启动 `run_obstacle_task:=false` 的滤波闭环。
2. 等待相机、IMU、滤波器、AMCL、move_base 全部正常。
3. 检查订阅关系和诊断。
4. 使用 `Ctrl+C` 正常退出。
5. 确认没有同名残留节点，再进行下一轮。

通过标准：五轮均无以下问题：

- `no IMU message received` 持续出现。
- `map` TF 长期不存在。
- 节点重复命名或旧节点残留。
- 远程 RViz 无法连接滤波节点。
- AMCL 或 move_base 错误订阅原 `/scan`。

### 12.2 完整障碍任务

前面全部通过后，在安全环境使用：

```bash
roslaunch allmovebase task_2026_obstacle_test.launch \
  run_obstacle_task:=true \
  start_ground_filter_experimental:=true \
  navigation_scan_topic:=/scan_ground_filtered
```

完整运行三次。通过标准：

- 三次均完成预定路径。
- 没有地面假障碍、频繁重规划或不合理绕行。
- 真实障碍响应符合预期。
- 定位没有累积漂移。
- 不需要人工调用 `clear_costmaps`。

## 13. 最终 GO/NO-GO 判定

### GO-OBSTACLE

必须同时满足：

```text
[ ] Gate 0 构建与单元测试通过
[ ] Gate 1 启动与订阅通过
[ ] Gate 2 三轮姿态测试通过
[ ] Gate 3 近距离障碍保留通过
[ ] Gate 4 动态障碍自动清除通过
[ ] Gate 5 建图现场 AMCL 三轮通过
[ ] Gate 6 步态测试三轮通过
[ ] Gate 7 连续启动与完整障碍任务三轮通过
```

全部勾选后，才建议把 `task_2026_obstacle_test.launch` 的默认参数改为滤波扫描。

### GO-ALL

在 `GO-OBSTACLE` 后，还需分别运行 navigation、pick-place 和 full task，每项至少三次无回归，才能逐步修改这些顶层 launch 的默认值。

### NO-GO

任一硬性 Gate 失败即为 `NO-GO`。保持：

```xml
<arg name="start_ground_filter_experimental" default="false"/>
<arg name="navigation_scan_topic" default="/scan"/>
```

失败不代表回退本次代码；滤波器仍可通过显式参数用于诊断和后续修改验证。

## 14. 验收结果回传模板

每完成一个 Gate，将以下内容回传给 Codex：

```text
Gate 编号：
日期/地点：
Git HEAD：
启动命令：
动作过程：
关键数据：
RViz 观察：
是否调用 clear_costmaps：
PASS / FAIL：
失败发生时间：
终端错误或警告：
截图/rosbag 文件名：
备注：
```

如果失败，只需回传失败 Gate，不要继续执行更高风险阶段。
